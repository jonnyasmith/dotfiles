# MUST live here, not in mise.toml: config-file discovery happens before
# mise.toml is read, so `auto_env` there has no effect. It is what loads
# mise.<os>.toml. Defaults to enabled in mise 2027.6.0.
auto_env = true
