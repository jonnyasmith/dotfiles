# winget package IDs installed by ./bootstrap.ps1 on native Windows. This list
# exists because mise has no `winget` manager, so Windows GUI apps cannot live
# in [bootstrap.packages]. Anything mise's registry can install is a [tools]
# entry in the shared mise.toml instead.
#
# One ID per line. Blank lines and `#` comments are ignored.

AgileBits.1Password
Git.Git
# Not a Fira Mono Nerd Font as on macOS: winget has no manifest for one under
# any publisher, so the two platforms differ.
DEVCOM.JetBrainsMonoNerdFont
Google.Chrome
Microsoft.PowerShell
Microsoft.PowerToys
Microsoft.VisualStudioCode

# Container runtime. Docker Desktop is deliberately absent.
RedHat.Podman-Desktop

# Registry-level scancode remaps (HKLM Scancode Map) apply at the logon screen
# with no process running; PowerToys Keyboard Manager only remaps while
# PowerToys is running.
RandyRants.SharpKeys
